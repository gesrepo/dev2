<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Connect2_Show__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>Internal Lookup to Connect2 Show.  This is set via insert / update trigger when Design Request Form is set.  It is used internally when adding show team member assignments.</description>
    <inlineHelpText>Internal Lookup to Connect2 Show.  This is set via insert / update trigger when Design Request Form is set.  It is used internally when adding show team member assignments.</inlineHelpText>
    <label>Connect2 Show</label>
    <referenceTo>GC_Connect2_Show__c</referenceTo>
    <relationshipLabel>Design Request Form Budgets</relationshipLabel>
    <relationshipName>Design_Request_Form_Budgets</relationshipName>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
