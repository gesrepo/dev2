/**
* @author Ethos Solutions - www.ethos.com
* @description Test code for Opportunity DAO
* 2016-04-13 : Original Version
**/
@isTest
public class GC_OpportunityDaoTest {
    public static testmethod void getOppByIdTest() {
        //Opportunity opp = GC_TestUtils.getTestOpportunity();
        //Opportunity methodValue = GC_OpportunityDao.getInstance().getOppById(opp.Id);
        //System.assertEquals(opp.Id, methodValue.Id, 'should pass if stringId and methodValue.Id match');
    }

    public static testmethod void getOppByIdsTest() {
        //Opportunity opp = GC_TestUtils.getTestOpportunity();
        //Set<String> setIds = new Set<String>();
        //setIds.add(opp.Id);
        //List<Opportunity> methodValue = GC_OpportunityDao.getInstance().getByOppIds(setIds);
        //System.assertEquals(1, methodValue.size(), 'The number of opp items in the set should match the methodValue size');
    }
}